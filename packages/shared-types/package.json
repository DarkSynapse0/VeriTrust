{
  "name": "@veritrust/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types — Credential, Issuer, VerificationResult, enums.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "echo 'shared-types: filled in Phase 1 task 4' && exit 0",
    "lint": "echo 'shared-types: filled in Phase 1 task 4' && exit 0",
    "typecheck": "echo 'shared-types: filled in Phase 1 task 4' && exit 0",
    "test": "echo 'shared-types: filled in Phase 1 task 4' && exit 0",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  }
}
